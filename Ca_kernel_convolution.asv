clear all
close all

%creating a calcium kernel
%CaNew = (1 - fr_time./tau).*Ca + (fr_time./tau)Cbaseline + sp_ht.*n_sp;
%%from Vogelstein, Paninski et al, J neurophys; DOI: 10.1152/jn.01073.2009

tau = .5;           %in s
fr_time = .2;       %in s
Cbaseline = .1;
Ca = Cbaseline;
n_frames = 100;
saved_Cas = zerso(1, n_frames);
for t_steps = 1:n_frames
    if t_step == 50
        
        
    CaNew = (1 - fr_time./tau).*Ca + (fr_time./tau)Cbaseline + sp_ht.*n_sp;
    
    
end